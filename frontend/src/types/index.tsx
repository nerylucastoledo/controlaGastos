export interface Card {
  name: string;
  color: string
  username: string;
}

export interface Category {
  name: string;
  username: string;
}

export interface People {
  name: string;
  username: string;
}